#!/sbin/runiscript
# SERVICE: daemon/mdev
# NAME: mdev
# DESCRIPTION:

setup(){
    iregister daemon
        iset need = system/pci
        iset respawn_pause = 1
        iset respawn_rate = 0
        iset respawn
        iexec daemon
    idone
}

daemon(){
    exec /sbin/mdev -df
}
